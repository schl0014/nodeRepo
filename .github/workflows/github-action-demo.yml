name: GitHub Actions Demo
on: [push]
jobs:
 Explore-GitHub-Actions:
   runs-on: ubuntu-latest
   steps:
      uses: actions/checkout@v3 
    - run: npm ci
    - run: npm test
    - run: echo "Good job!"
